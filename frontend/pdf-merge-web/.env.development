# next dev：同源 /api/v1/pdf/merge 由 route 转发到 MERGE_API_PROXY_TARGET（默认线上），此时本仓库里改的 application.yml 不会作用到该上游。
# 要让浏览器命中本机 Spring：例如 NEXT_PUBLIC_MERGE_API_BASE=http://127.0.0.1:8080/api/v1/pdf/merge
NEXT_PUBLIC_MERGE_API_BASE=http://127.0.0.1:8080/api/v1/pdf/merge
