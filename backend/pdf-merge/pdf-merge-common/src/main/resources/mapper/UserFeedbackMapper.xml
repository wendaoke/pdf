<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.pdfmerge.common.mapper.UserFeedbackMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO user_feedback (
            owner_id, contact, category, content, context_json, status, created_at, updated_at
        ) VALUES (
            #{ownerId}, #{contact}, #{category}, #{content}, #{contextJson}, #{status}, #{createdAt}, #{updatedAt}
        )
    </insert>
</mapper>
